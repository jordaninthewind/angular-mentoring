import { CustomNumberValidatorDirective } from './custom-number-validator.directive';

describe('CustomNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
